# Upgrade to Full GrayMatter

When ready for hosted GrayMatter:
1. Run `gm-activate --token <token>`.
2. Use your starter 500 credits on https://valkyrlabs.com.
3. Point clients to hosted API base URL after activation.
